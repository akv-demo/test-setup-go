name: CI

on:
  push:
  pull_request:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

name: CI

on:
  push:
  pull_request:

  workflow_dispatch:

jobs:
  find-default-go:
    name: 'Find default go version'
    runs-on: windows-latest
    
  steps:
    - run: echo "aaaa"> /tmp/env

    - uses: actions/setup-go@v4
      with:
      go-version: '1.20'
      cache-dependency-path: go.sum /tmp/env
    - run: go run hello.go    
    - run: echo "bbbb"> /tmp/env
    
    - uses: actions/setup-go@v4
      with:
      go-version: '1.20'
      cache-dependency-path: go.sum /tmp/env  
   
   - run: go run hello.go    

